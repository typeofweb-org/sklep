# Migration `20201110174348-add-tax-rate`

This migration has been generated by Michal Miszczyszyn at 11/10/2020, 6:43:48 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Product" ADD COLUMN "taxRate" Decimal(65,30)   
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201101150422-sklep..20201110174348-add-tax-rate
--- datamodel.dml
+++ datamodel.dml
@@ -1,12 +1,12 @@
 generator client {
   provider = "prisma-client-js"
-  // previewFeatures = []
+  // previewFeatures = ["nativeTypes"]
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model User {
   id Int @default(autoincrement()) @id
@@ -45,8 +45,10 @@
   regularPrice Int
   discountPrice Int?
+  taxRate Float?
+
   type ProductType @default(SINGLE)
   userId Int
   addedBy User @relation(fields: [userId], references: [id])
```


